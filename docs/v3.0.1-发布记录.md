# v3.0.1 发布记录

> 日期：2026-05-29
> 操作：将 v3.0.1 合并到 `main`

## 概述

v3.0.1 针对 [v3.0.0 代码审查](v3-issues.md)发现的 8 个问题进行修复，三套实现（Python / macOS zsh / Windows PowerShell）行为保持一致。修复在分支 `claude/silly-lehmann-2a55f6` 上完成（提交 `23fce05`），并于 2026-05-29 以**快进（fast-forward）**方式合并到 `main`。

- 逐条变更说明见 [CHANGELOG.md](../CHANGELOG.md)。
- 问题与修复状态对照见 [docs/v3-issues.md](v3-issues.md) 顶部的「修复状态」表。

## 合并信息

| 项 | 值 |
|----|----|
| 目标分支 | `main` |
| 来源分支 | `claude/silly-lehmann-2a55f6` |
| 合并前 `main` | `5f675bc`（v3.0.0 + 审查记录） |
| 合并后 `main` | `23fce05`（v3.0.1） |
| 合并方式 | fast-forward（无合并提交、无冲突） |

## 修复摘要

| # | 等级 | 文件 | 修复内容 |
|---|------|------|----------|
| 1 | 🔴 | `图片转PPTX.command` | 新增 EXIF 方向处理（`/usr/bin/perl` 解析），竖拍照片不再变形 |
| 2 | 🔴 | `图片转PPTX.command` | `core.xml` 标题做 XML 转义，特殊字符文件夹名不再生成损坏文件 |
| 3 | 🟡 | `images_to_pptx.py` | `--recursive` 改用 `os.walk(followlinks=False)`，不再跟随符号链接 |
| 4 | 🟡 | `images_to_pptx.py` | 缺号检测改用集合差集 |
| 5 | 🟢 | `images_to_pptx.py` | 变量 `ignored_numbered` 重命名为 `skipped_unnumbered` |
| 6 | 🟢 | `图片转PPTX-Windows.cmd` | 标题改用文件夹名并转义，与 Python 版一致 |
| 7 | 🟢 | `图片转PPTX-Windows.cmd` | 移除冗余自定义函数 `Join-Path2` |
| 8 | 🟢 | `images_to_pptx.py` | 复核为**误报**：文件本就使用 4 空格缩进，未改动 |

## 验证

- **Python、macOS zsh 版**：端到端运行（生成 PPTX 并解析校验占位与 XML），EXIF 占位结果与 Python 版逐像素一致。
- **Windows PowerShell 版**：本机无 PowerShell 未实跑，已通过静态检查（圆括号 / 花括号 / here-string 配平、无 `Join-Path2` 残留）确认。

## 后续清理（可选）

- 分支 `claude/silly-lehmann-2a55f6` 已全部并入 `main`，可安全删除。
- 该分支的旧 worktree（路径仍指向旧文件夹名 `PPT转图片脚本`）已被 git 标记为 `prunable`，可用 `git worktree prune` 清理。
